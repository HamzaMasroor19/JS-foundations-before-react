function orderFood(callback) {
    setTimeout(() => {
        console.log("Food ordered");
        callback();
    }, 1000);
}

function prepareFood(callback) {
    setTimeout(() => {
        console.log("Food prepared");
        callback();
    }, 1000);
}

function cookFood(callback) {
    setTimeout(() => {
        console.log("Food cooked");
        callback();
    }, 1000);
}

function deliverFood(callback) {
    setTimeout(() => {
        console.log("Food delivered");
        callback();
    }, 1000);
}

orderFood(function () {
    prepareFood(function () {
        cookFood(function () {
            deliverFood(function () {
                console.log("Order completed");
            });
        });
    });
});
//Callback Hell:
// When multiple asynchronous callbacks become deeply nested inside one another, making the code difficult to read, understand, maintain, and debug.

// Pyramid of Doom:
// The pyramid-shaped structure created by deeply nested callbacks.

// Inversion of Control:
// When you pass a callback to another function, you give that function control over when and how your callback is executed.


// Another Simple Example
//Login → Get User → Get Posts → Show Posts
login(function () {

    getUser(function () {

        getPosts(function () {

            showPosts(function () {

                console.log("Done");

            });

        });

    });

});
